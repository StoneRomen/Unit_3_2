// Отримати від користувача 3 рядки та вивести їх у довільному порядку однією командою (шаблонні рядки);


const str1 = 'JavaScript';
const str2 = 'know';
const str3 = 'I dont';

const strings = [str1, str2, str3];

console.log (strings[2],strings[1], strings[0],)
